# Claude Code Rules

## Base Rules
Follow all development rules defined in AGENTS.md.

## Claude Code Specific
- Never run git commit / push / merge without explicit user confirmation
- Before reviewing, always run `git branch` and `pwd` to confirm your location